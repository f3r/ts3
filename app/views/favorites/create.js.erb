<%= page_replace("##{dom_id(@parent, 'favorite')}",
  :partial => 'search/button_favorite', :locals => { :favorable => @parent }) %>

<%= open_modal :share_place_modal, 'search/sharing_options', :place => @parent %>
stButtons.locateElements();
