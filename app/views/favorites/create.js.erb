<%= page_replace("#favorite_#{@place['id']}",
      :partial => 'places/button_favorite',
      :locals => { :place_id => @place['id'], :favorited => @place['favorited'] }) %>

<%= open_modal :share_place_modal, 'places/sharing_options' %>
stButtons.locateElements();
